# dots

Personal dotfiles for Linux and macOS, intended to be symlinked into `$HOME`.
Assumes this repo is checked out at `~/dots` and that `nix` is installed.
Includes configuration for neovim, tmux, ghostty, jj/jjui, pi and more. Probably
only works on my machines!

## Usage

```sh
bash make-links
```
